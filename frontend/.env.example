# Leave empty when FastAPI serves the frontend on the same domain.
# For a separate Vercel frontend, use the public backend origin without a
# trailing slash, for example: https://monopoly-api.example.com
VITE_API_BASE_URL=
